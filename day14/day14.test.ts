import { day14 } from "./day14";

describe("Day 14", () => {
  it("S1 should work correctly", () => {
    expect(day14(1)).toBe(719);
  });
  it("S2 should work correctly", () => {
    expect(day14(2)).toBe(23390);
  });
});
